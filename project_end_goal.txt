Need 1: My team at work have different gemini accounts. They will be using this software to effectively streamline the 3-statement-modelling process. However,
some of my team are not very code-linguistic and so, I don't want for them to have to manually get an API key then add it to their .env file - that will sound like gibberish instructions for them.

Need 2: I have not been able to decide between having this program on the cloud, or a local executable. Either way, there is no database needs - this is a lightweight software. The user will just be able to click an 'upload PDF(s)' button (up to LIMIT at a time). Once it's uploaded, I want a carousel where users can set parameters for each individual pdf. For example: I upload 3 pdfs, pdf_1, pdf_2 and pdf_3. Once uploaded, there are three dots ( for each pdf ) indicating where in the pdf carousel i am. PDF_1 is displayed, with input boxes to the side and a forwards/backwards arrow to navigate between the pdfs. The input boxes are [For you, the LLM reading this - the following boolean language here indicates which boxes can be simulatenously populated with data]: [Starting page number OR Ending page number (both optional)] XOR [Specific pages (expects comma separated page numbers e.g. 1,2,3-7,10-15 where hyphen indicates range)] AND [Model (a model selection - by default, gemini 2.5 pro. For each model (gemini 2.5 pro, gemini 3.1))] - look at ui-mockup.svg for what I was thinking

Need 3: For multiple pdfs, all must be passed through llamaParse before the excel AI sees them. Further, it might be too context heavy for one prompt. Perhaps do each markdown one at a time, then have a final AI combine them all WITH FORMULA   